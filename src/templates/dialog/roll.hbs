<div class="bdtiles-dialog-content">
  <h2>{{ talent.name }} ({{ talent.value }})</h2>
  <section class="bdtiles-dialog-modifiers">
    <div class="bdtiles-dialog-modifier">
      <label
        for="bdtiles-dialog-modifier-difficulty"
        >{{ localize 'BDTILES.Global.difficulty' }}</label
      >
      <select id="bdtiles-dialog-modifier-difficulty" name="difficulty">
        {{#each difficultyLevels as |difficulty|}}
        <option value="{{ difficulty.value }}">
          {{ localize (concat 'BDTILES.Dialog.difficulty.' difficulty.label) }}
          ({{ difficulty.value }})
        </option>
        {{/each}}
      </select>
    </div>
  </section>
</div>
<div class="dialog-buttons">
  {{#each buttons as |button id|}}

  <button
    class="dialog-button {{ cssClass }}"
    data-button="{{ id }}"
    {{disabled
    button.disabled}}
  >
    {{{button.icon}}} {{{button.label}}}
  </button>
  {{/each}}
</div>
